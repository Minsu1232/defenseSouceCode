using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GuideArrowAnimation : MonoBehaviour
{
   //[SerializeField] private RectTransform arrowTransform; // 화살표 UI의 RectTransform
   // [SerializeField] private float moveDistance = 50f; // 이동할 거리
   // [SerializeField] private float duration = 0.5f; // 애니메이션 지속 시간

   // private void Start()
   // {
   //     StartArrowAnimation();
   // }

   // private void StartArrowAnimation()
   // {
   //     // 위아래로 반복해서 움직이는 애니메이션
   //     arrowTransform.DOMoveY(arrowTransform.position.y + moveDistance, duration)
   //         .SetEase(Ease.InOutSine) // 부드럽게 움직이는 이징 함수
   //         .SetLoops(-1, LoopType.Yoyo); // 무한 반복(Yoyo는 위아래로 반복)
   // }
}
